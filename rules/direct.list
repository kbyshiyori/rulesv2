# direct: traffic that must bypass the China node and use the local connection.
#
# Client-agnostic intent format (comma-separated):
#   ip-cidr,<IPv4 network>
#   domain-suffix,<domain>
#   protocol-port,<tcp|udp>,<destination port>
#
